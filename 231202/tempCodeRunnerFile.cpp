rep(k,n){
	//     if(k == 0){
	//          ans[a[k].second] = sum;
	//          sum += (ll)a[k].first;
	//          res = a[k].first;
	//     }else{
	//         if(res != a[k].first) {
	//             ans[a[k].second] = sum;
	//             r = 1;
	//         }else{
	//             sum -= (ll)res*r;
	//             r++;
	//             ans[a[k].second] = sum;
	//         }
  //   		sum += (ll)a[k].first*r;
  //   		res = a[k].first;
	//     }
	    
	// }